<%= form_with(model: doohickey, local: true) do |form| %>
<% if doohickey.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(doohickey.errors.count, "error") %> prohibited this doohickey from being saved:</h2>

    <ul>
    <% doohickey.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
    </ul>
  </div>
<% end %>

<div class="field">
  <%= form.label :title %>
  <%= form.text_field :title %>
</div>

<div class="actions">
  <%= form.submit %>
</div>
<% end %>
